/*
  SocialProof.jsx — Honest social proof section.
  
  IMPORTANT — Assignment constraint:
  No fabricated testimonials, fake user counts, or fake company logos.
  Only real, verifiable proof will be added here.
  
  Planned content (Stage 2, with real data only):
  - Genuine student quotes (from beta testers, if available)
  - Honest metrics (only if real: e.g., "Built by students, for students")
  - If no real proof is available yet, this section can be replaced
    with a "Join the waitlist" or "Be among the first" framing instead
*/
function SocialProof() {
  return (
    <section id="social-proof" className="section">
      {/* Social proof content will be implemented in Stage 2 — real data only */}
    </section>
  )
}

export default SocialProof
