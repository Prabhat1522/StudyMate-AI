/*
  src/hooks/ — Custom React hooks
  
  This folder contains reusable hooks that encapsulate
  stateful logic, keeping components clean and focused
  on rendering.
  
  Examples of what will live here (added as needed):
  - useScrollPosition.js  → Detects scroll position (for sticky Navbar)
  - useMediaQuery.js      → Detects screen size breakpoints
  - useIntersection.js    → Triggers animations when elements enter viewport
*/

// Intentionally empty — populated as needed in later stages
